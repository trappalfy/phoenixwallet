# OS
.DS_Store

# Dependencies (root site + extension/, both match via this one pattern)
node_modules/

# Build output (root site + extension/, both match via this one pattern)
dist/

# extension/ lives on this branch, not on main. store/build/ (the submission
# zip) stays ignored — it's a regenerable artifact of `npm run package`.
extension/store/build/
