import Marquee from './Marquee'

// Networks section — just the scrolling marquee now (stat grid + rake removed).
export default function Numbers() {
  return (
    <section id="networks" className="relative px-6 pt-16 pb-8 md:pt-20 md:pb-10">
      <Marquee />
    </section>
  )
}
